/*// Has a function named calcAvgSquare that can calculate the average of 3 numbers times the square of the first number. The formula is:
function calcAvgSquare(x,y,z) {
  let AvgSquare = ((x+y+z)/3)* (num1 ** 2)
    return AvgSquare
}

let num1 = Number(prompt("Please enter a number"))
let num2 = Number(prompt("Please enter a second number"))
let num3 = Number(prompt("Please enter a third number"))

let calculator_answer = calcAvgSquare(num1,num2,num3)

// Outputs the answer from the main program (not from the function) using an alert with a template literal using this format: 
alert("The answer is " + calculator_answer)*/